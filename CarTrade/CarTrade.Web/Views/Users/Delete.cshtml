@model UserDetailViewModel
@{
    ViewData["Title"] = "Remove user";
}

<h2>@ViewData["Title"]</h2>

<h3>@Model.Name</h3>

<div>
    <a href="/users/destroy/@Model.UserId" onclick="return confirm('Are you sure you want to delete this item?');" class="btn btn-danger">Remove user</a>
</div>